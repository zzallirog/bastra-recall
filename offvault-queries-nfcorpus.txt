# Absent-topic queries for NFCorpus (BEIR) — the mirror of offvault-queries.txt.
#
# NFCorpus is nutrition and medicine, so the absent set has to come from a
# different domain than the one used against a personal dev vault. These are
# software/ops topics plus the bare operational strings #249 reported live.
# The cooking and vitamin queries in offvault-queries.txt would be IN domain
# here — that is the point of keeping two lists rather than one.
# Written before looking at any result, kept verbatim afterwards.
reciprocal rank fusion damping constant
systemd user unit socket activation ordering
typescript discriminated union exhaustiveness check
hyprland window rule floating workspace
git rebase interactive autosquash fixup
btrfs subvolume snapshot rollback
nginx reverse proxy websocket upgrade header
postgres vacuum full table bloat
docker layer cache invalidation order
kubernetes pod disruption budget eviction
rust borrow checker lifetime elision
wireguard peer allowed ips routing
zsh completion function compdef
ffmpeg concat demuxer stream copy
sqlite write ahead log checkpoint
tmux pane synchronize input
regex lookbehind variable length
oauth refresh token rotation
elasticsearch shard allocation awareness
python asyncio event loop starvation
vim macro register recursive
llvm inline threshold heuristic
tls certificate pinning mobile client
raid5 write hole battery backup
xorg compositor vsync tearing
grep perl regex null byte
makefile pattern rule prerequisite order
ssh agent forwarding socket hijack
json schema additionalProperties false
cron expression day of week overlap
# Operational strings — no topic at all.
ls -la
chmod 755
kill -9
cd ..
docker compose down
curl -s localhost
mv file.txt backup.txt
sudo systemctl restart
git push --force
tail -f log
