{
  "name": "bastra-recall-monorepo",
  "version": "0.9.2",
  "description": "Workspace root for bastra-recall: OSS daemon, MCP server, and shared ChatGPT/Codex/Claude skill — the open foundation under the Bastra Mac app.",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/core",
    "packages/daemon",
    "packages/statusline",
    "packages/bastra-recall",
    "packages/eval"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "skill:build": "node scripts/build-skill-projections.mjs",
    "check:types": "npm run check:types --workspaces --if-present",
    "pretest": "npm run build --workspace=@bastra-recall/core",
    "test": "node --import tsx --import ./scripts/test-env.mjs --test packages/core/__tests__/*.test.ts packages/daemon/__tests__/*.test.ts packages/eval/__tests__/*.test.ts tools/__tests__/*.test.mjs",
    "eval:lift": "npm run lift --workspace=@bastra-recall/eval",
    "test:update": "npm run test:update --workspace=@bastra-recall/daemon",
    "smoke": "npm run smoke --workspace=@bastra-recall/daemon",
    "smoke:telemetry": "npm run smoke:telemetry --workspace=@bastra-recall/daemon",
    "pack:check": "npm pack --dry-run --workspace=@bastra-recall/core && npm pack --dry-run --workspace=@bastra-recall/statusline && npm pack --dry-run --workspace=@bastra-recall/daemon && npm pack --dry-run --workspace=bastra-recall",
    "release:version": "node scripts/bump.mjs"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  },
  "overrides": {
    "esbuild": "0.28.1",
    "hono": "^4.12.27",
    "@hono/node-server": "^2.0.5",
    "tmp": "^0.2.6",
    "fast-uri": "^3.1.3",
    "body-parser": "^2.3.0"
  },
  "homepage": "https://bastra.io"
}
